# npm ( Node Package Manager)

## Package Installation

> $ npm install [option] \<package>

* alias: `i`
* option
  + `--global`, `-g`
  + `--save-prod`
  + `--save-dev`
  + `--save-optional`
  + `--save-exact`
  + `--no-save`

## Display Installed Package

> $ npm i -g --depth 0
